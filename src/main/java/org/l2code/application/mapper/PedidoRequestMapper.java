/*
package org.l2code.application.mapper;

import com.seumanoel.api.model.RequisicaoEmpacotamento;
import org.l2code.application.dto.RequisicaoEmpacotamentoDTO;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;
import org.mapstruct.ReportingPolicy;

@Mapper(componentModel = "spring", unmappedTargetPolicy = ReportingPolicy.ERROR, uses = {PedidoMapper.class})
public interface PedidoRequestMapper {
    RequisicaoEmpacotamentoDTO toRequisicaoEmpacotamentoDTO(RequisicaoEmpacotamento source);
}

*/
